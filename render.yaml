services:
  - type: static_site
    name: mypro-1
    rootDir: client
    buildCommand: npm install && npm run build
    publishDir: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html